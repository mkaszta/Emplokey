@model IEnumerable<Emplokey_WebAdmin.Auths>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_pc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Auth_key)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Device)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_pc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Auth_key)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Device)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
